# Nunca vai pra imagem: node_modules é reinstalado dentro do build.
node_modules
server/node_modules

# O banco de dados agora é PostgreSQL, num container/volume próprios
# (toolbox45-db) — nunca fica dentro da imagem do backend nem do frontend.
server/backup/
/backup/

# Documentação e planilhas — não afetam a aplicação em si.
*.md
*.docx
*.xlsx

# Metadados de versionamento/editor
.git
.gitignore
.vscode
.idea

# Backups avulsos (se algum dia voltarem a existir)
*.bak
*.bak_*
